import {notFound} from "next/navigation";import {countriesAlphabetical} from "@/data/global-dog-ownership-index/countries";import {CountryProfile} from "@/components/global-index/CountryProfile";import {pageMetadata} from "@/lib/metadata";
export const dynamicParams=false;export function generateStaticParams(){return countriesAlphabetical.map(c=>({country:c.slug}))}export async function generateMetadata({params}:{params:Promise<{country:string}>}){const {country:slug}=await params;const c=countriesAlphabetical.find(x=>x.slug===slug);return c?pageMetadata(`Dog Ownership in ${c.name}: Global Comparison | Dog Haven Group`,c.description,`world-atlas/global-dog-ownership-index/${c.slug}`):{}}export default async function Page({params}:{params:Promise<{country:string}>}){const {country:slug}=await params;const c=countriesAlphabetical.find(x=>x.slug===slug);if(!c)notFound();return <CountryProfile country={c}/>}
